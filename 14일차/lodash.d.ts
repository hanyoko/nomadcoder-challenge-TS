declare module "lodash" {
    function head(array: any): typeof array[0]
    function hasIn(key: Object): boolean
    function isBoolean(value: boolean): boolean
    function toString(value: string): string
    function split(string: string | number): string
    function hasPath(object: Object): boolean
    function filter(Array: string): string
    function every(Array: boolean): boolean
    function map(Array: string): string
    }